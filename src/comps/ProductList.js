import React from 'react';
import styled, { keyframes } from 'styled-components';

const ProductListContainer = styled.div `

`;

const ProductList = () => {
    return (
        <ProductListContainer>P-list</ProductListContainer>
    );
}

export default ProductList;